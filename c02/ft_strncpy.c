#include <stdio.h>
#include <unistd.h>

char	*ft_strncpy(char *dest, char *src, unsigned int n);

int		main(void)
{
	char	dest;
	char	str;
	int		length;

	dest = "my name is";
	str = "brad lee";
	length = 4;
	ft_strncpy(dest, src, n);
	return (0);
}

char	*ft_strncpy(char *dest, char *src, unsigned int n);
{
	
}
